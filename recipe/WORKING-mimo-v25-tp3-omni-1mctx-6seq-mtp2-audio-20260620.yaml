recipe_version: "1"
name: WORKING-MiMo-V2.5-NVFP4-TP3-OMNI-1MCTX-6SEQ-MTP2-AUDIO-20260620
description: >
  Frozen working TP=3 Omni/vision/video/audio + MTP2 + 1M context path for
  lukealonso/MiMo-V2.5-NVFP4 on Bluey+Reddie+Asusi. This is the audio-enabled
  image variant of the Omni+MTP2 working recipe. Verified by Codex on
  2026-06-20: server boots, MiMoV2OmniMTPModel resolves, 1M context remains,
  text smoke passes, image smoke passes, and WAV audio smoke passes.

model: lukealonso/MiMo-V2.5-NVFP4
container: vllm-mimo-omni-mtp2-1m-audio-exp:20260620
cluster_only: true
solo_only: false

mods:
  - mods/drop-caches
  - mods/ray-keep-node-nccl-hca
  - mods/fix-prometheus-instrumentator-router
  - mods/fix-mimo-v2-vllm
  - mods/fix-modelopt-mixed-mxfp8
  - mods/mimo-v2-tp3-virtual-heads

defaults:
  port: 8000
  host: 0.0.0.0
  served_model_name: MiMo-V2.5-NVFP4
  model_path: /root/.cache/huggingface/hub/models--lukealonso--MiMo-V2.5-NVFP4/snapshots/a147dd04d6cf861e43b2d783dcde23b53ab7ee68
  tensor_parallel: 3
  pipeline_parallel: 1
  gpu_memory_utilization: 0.89
  max_model_len: 1000000
  max_num_batched_tokens: 8192
  max_num_seqs: 6

env:
  TORCH_CUDA_ARCH_LIST: "12.1a"
  PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True
  VLLM_MIMO_V2_TP3_VIRTUAL_HEADS: "1"
  VLLM_NVFP4_GEMM_BACKEND: flashinfer-cutlass
  VLLM_USE_FLASHINFER_MOE_FP4: "1"
  VLLM_FLASHINFER_MOE_BACKEND: throughput
  VLLM_USE_RAY_V2_EXECUTOR_BACKEND: "0"
  VLLM_USE_RAY_COMPILED_DAG_OVERLAP_COMM: "0"
  VLLM_ALLOW_LONG_MAX_MODEL_LEN: "1"
  NCCL_CUMEM_ENABLE: "0"
  NCCL_NVLS_ENABLE: "0"
  NCCL_NTHREADS: "32"
  NCCL_NSOCKS_PERTHREAD: "2"
  NCCL_BUFFSIZE: "8388608"
  NCCL_CROSS_NIC: "1"
  NCCL_IB_GID_INDEX: "3"
  NCCL_IB_ADDR_RANGE: 192.168.100.0/24,192.168.101.0/24,192.168.102.0/24

command: |
  vllm serve {model_path} \
      --served-model-name {served_model_name} \
      --trust-remote-code \
      --tensor-parallel-size {tensor_parallel} \
      --pipeline-parallel-size {pipeline_parallel} \
      --distributed-executor-backend ray \
      --load-format instanttensor \
      --hf-overrides '{{"architectures":["MiMoV2OmniForCausalLM"]}}' \
      --limit-mm-per-prompt '{{"image":4,"video":1,"audio":1}}' \
      --mm-encoder-tp-mode data \
      --attention-backend triton_attn_diffkv \
      --kv-cache-dtype fp8_e4m3 \
      --gpu-memory-utilization {gpu_memory_utilization} \
      --max-model-len {max_model_len} \
      --max-num-batched-tokens {max_num_batched_tokens} \
      --max-num-seqs {max_num_seqs} \
      --block-size 32 \
      --enable-prefix-caching \
      --enable-chunked-prefill \
      --speculative-config '{{"method":"mtp","num_speculative_tokens":2}}' \
      --no-async-scheduling \
      --enable-auto-tool-choice \
      --tool-call-parser mimo \
      --reasoning-parser mimo \
      --host {host} \
      --port {port}
